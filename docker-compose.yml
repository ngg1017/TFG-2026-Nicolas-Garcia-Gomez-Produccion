services:
  #Aplicacion de Reflex
  reflex_app_tfg:
    build: .
    restart: always
    #Le pasamos las variables de entorno para que se conecte
    env_file:
      - .env
    ports:
      - "3000:3000"
      - "8000:8000"